# notebook-api has NO public route.
# It is only accessible internally via the notebook-ui nginx proxy at /api/
# This prevents external callers from spoofing X-Forwarded-User
# or directly calling /user-token/{username} to harvest MaaS tokens.
