apiVersion: v2
name: model-registry
description: >
  Deploys OpenShift AI Model Registry instances with self-contained Postgres.
  Each entry in modelRegistries creates: Secret, PVC, Deployment, Service, ModelRegistry CR.
type: application
version: 0.1.0
appVersion: 0.1.0
