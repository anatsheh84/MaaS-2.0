apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kubernetes-mcp-server
  namespace: openshift-gitops
  annotations:
    argocd.argoproj.io/sync-wave: "8"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    # OCI Helm chart — cannot be copied to git, sourced from registry directly
    - repoURL: oci://ghcr.io/containers/charts/kubernetes-mcp-server
      targetRevision: 0.1.0
      path: .
      helm:
        valueFiles:
          - $values/charts/kubernetes-mcp-server/values.yaml
    # Values file now lives in our own repo
    - repoURL: {{ .Values.bootstrap.repoURL }}
      targetRevision: {{ .Values.bootstrap.targetRevision }}
      ref: values
  destination:
    name: in-cluster
    namespace: lls-demo
  syncPolicy:
    automated: {}
    syncOptions:
      - CreateNamespace=true
