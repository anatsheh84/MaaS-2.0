apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: model-registry
  namespace: openshift-gitops
  annotations:
    # Wave 6 — after openshift-ai (wave 5) has deployed the ModelRegistry operator component
    argocd.argoproj.io/sync-wave: "6"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: {{ .Values.bootstrap.repoURL }}
    targetRevision: {{ .Values.bootstrap.targetRevision }}
    path: charts/model-registry
    helm:
      valueFiles:
        - values.yaml
        - values-llmaas.yaml
  destination:
    name: in-cluster
    namespace: rhoai-model-registries
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  # The ModelRegistry operator injects image/resource defaults into spec after creation.
  # Ignore these operator-managed fields to prevent a permanent OutOfSync loop.
  ignoreDifferences:
    - group: modelregistry.opendatahub.io
      kind: ModelRegistry
      jsonPointers:
        - /spec/rest/image
        - /spec/rest/resources
        - /spec/grpc/image
        - /spec/postgres/image
        - /metadata/annotations/argocd.argoproj.io~1sync-wave
